import { useState } from 'react';

interface Props {
  mapReady: boolean;
}

export default function ForecastLine(props: Props) {
  // const {} = useState<>
  const { mapReady, Amap, mapInstance } = props;

  return <div></div>;
}
